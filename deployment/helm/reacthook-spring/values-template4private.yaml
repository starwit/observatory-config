# Private values for helm install.

mariadb:
  auth:
    rootPassword: root #change
    password: smartparkingconfig #change

github:
  registry:
    username: change
    pat: change

oauth:
  clientid: change
  clientsecret: change
